"""Shared library for the trimodal spherical-loss investigation.

Modules: config, run_utils, sphere (directional statistics), data (Sketchy +
mock fallback), model (frozen CLIP + LoRA sketch path), losses (baseline /
frechet / triangle / vmf), train (loop + retrieval eval + diagnostics).
"""
